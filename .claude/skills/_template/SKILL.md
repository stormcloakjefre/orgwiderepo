---
name: skill-name
description: One sentence stating what this does and exactly when to invoke it (and when not to). This is the only text used to decide whether to trigger the skill.
---

# Skill Name

## When to use

- Bullet the concrete triggers (a phrase the user says, a file type, a
  recurring task).

## When not to use

- Bullet the cases this skill should not fire for, if any are likely to
  be confused with it.

## Steps

1. Keep this to the minimum steps needed to complete the task correctly.
2. Prefer pointing at existing project tooling/scripts over new logic.
3. Avoid optional branches for scenarios that haven't come up yet.
